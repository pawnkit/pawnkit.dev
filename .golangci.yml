version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - gosec
    - staticcheck
    - unconvert
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec

formatters:
  enable:
    - gofumpt
